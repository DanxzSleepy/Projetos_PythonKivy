
### **Como rodar:**
1. Salve em `contatos_evolucao.py`
2. Execute: `python contatos_evolucao.py`

### **Código completo testável:**
```python
class Contato:
    def __init__(self, nome, telefone, email):
        self.nome = nome
        self.telefone = telefone
        self.email = email

class GerenciadorContatos:
    def __init__(self):
        self.contatos = []
    
    def adicionar_contato(self, contato):
        self.contatos.append(contato)
        print(f"Contato '{contato.nome}' adicionado com sucesso.")
    
    def listar_contatos(self):
        print("\n--- Lista de Contatos ---")
        if not self.contatos:
            print("Nenhum contato cadastrado.")
        else:
            for contato in self.contatos:
                print(f"Nome: {contato.nome}, Telefone: {contato.telefone}, Email: {contato.email}")
        print("---\n")
    
    def pesquisar_por_nome(self, nome):
        resultados = [contato for contato in self.contatos if nome.lower() in contato.nome.lower()]
        if not resultados:
            print(f"Nenhum contato encontrado com o nome '{nome}'.")
        else:
            print(f"\n--- Resultados para '{nome}' ---")
            for contato in resultados:
                print(f"Nome: {contato.nome}, Telefone: {contato.telefone}, Email: {contato.email}")
            print("---\n")
        return resultados

# Teste do sistema
print("=== SISTEMA DE GERENCIAMENTO DE CONTATOS ===")
gerenciador = GerenciadorContatos()

# Adicionar contatos
gerenciador.adicionar_contato(Contato("Ana Silva", "11987654321", "ana.silva@email.com"))
gerenciador.adicionar_contato(Contato("Bruno Santos", "21912345678", "bruno.santos@email.com"))
gerenciador.adicionar_contato(Contato("Carlos Oliveira", "31998765432", "carlos.oliveira@email.com"))
gerenciador.adicionar_contato(Contato("Ana Clara", "41912349876", "ana.clara@email.com"))

# Listar todos
gerenciador.listar_contatos()

# Pesquisar
gerenciador.pesquisar_por_nome("Ana")
gerenciador.pesquisar_por_nome("Santos")
gerenciador.pesquisar_por_nome("João")
```

### **Resultado esperado:**
```
=== SISTEMA DE GERENCIAMENTO DE CONTATOS ===
Contato 'Ana Silva' adicionado com sucesso.
Contato 'Bruno Santos' adicionado com sucesso.
Contato 'Carlos Oliveira' adicionado com sucesso.
Contato 'Ana Clara' adicionado com sucesso.

--- Lista de Contatos ---
Nome: Ana Silva, Telefone: 11987654321, Email: ana.silva@email.com
Nome: Bruno Santos, Telefone: 21912345678, Email: bruno.santos@email.com
Nome: Carlos Oliveira, Telefone: 31998765432, Email: carlos.oliveira@email.com
Nome: Ana Clara, Telefone: 41912349876, Email: ana.clara@email.com
---

--- Resultados para 'Ana' ---
Nome: Ana Silva, Telefone: 11987654321, Email: ana.silva@email.com
Nome: Ana Clara, Telefone: 41912349876, Email: ana.clara@email.com
---

--- Resultados para 'Santos' ---
Nome: Bruno Santos, Telefone: 21912345678, Email: bruno.santos@email.com
---

Nenhum contato encontrado com o nome 'João'.
```
