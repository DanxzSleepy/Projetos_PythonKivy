
### **Como rodar:**
1. Salve o código em um arquivo `frete_corrigido.py`
2. Execute no terminal: `python frete_corrigido.py`

### **Código completo testável:**
```python
def calcular_frete(distancia_km, peso_kg, tipo_entrega):
    tabela_precos = [10.00, 15.50, 20.00]
    
    if not 0 <= tipo_entrega <= 2:
        return "Erro: Tipo de entrega inválido."
    
    if peso_kg <= 0:
        return "Erro: Peso deve ser maior que zero."
    
    custo_base = distancia_km * tabela_precos[tipo_entrega]
    
    if peso_kg > 50:
        custo_peso = peso_kg * 0.20
    elif peso_kg > 10:
        custo_peso = peso_kg * 0.10
    else:
        custo_peso = peso_kg * 0.05
    
    custo_total = custo_base + custo_peso
    
    if distancia_km > 100 and tipo_entrega == 1:
        custo_total *= 1.10  # Acréscimo de 10% para entrega expressa acima de 100km
    
    return round(custo_total, 2)

# Testes
print("=== TESTES DO SISTEMA DE FRETE ===")
print(f"Cenário 1 (50km, 15kg, Normal): R$ {calcular_frete(50, 15, 0)}")
print(f"Cenário 2 (20km, 60kg, Urgente): R$ {calcular_frete(20, 60, 2)}")
print(f"Cenário 3 (150km, 5kg, Expresso): R$ {calcular_frete(150, 5, 1)}")
print(f"Cenário 4 (30km, 8kg, Normal): R$ {calcular_frete(30, 8, 0)}")
print(f"Cenário 5 (100km, 0kg, Normal): {calcular_frete(100, 0, 0)}")
print(f"Cenário 6 (50km, 15kg, Tipo 5): {calcular_frete(50, 15, 5)}")
```

### **Resultado esperado:**
```
=== TESTES DO SISTEMA DE FRETE ===
Cenário 1 (50km, 15kg, Normal): R$ 501.5
Cenário 2 (20km, 60kg, Urgente): R$ 412.0
Cenário 3 (150km, 5kg, Expresso): R$ 2337.5
Cenário 4 (30km, 8kg, Normal): R$ 300.4
Cenário 5 (100km, 0kg, Normal): Erro: Peso deve ser maior que zero.
Cenário 6 (50km, 15kg, Tipo 5): Erro: Tipo de entrega inválido.
```
